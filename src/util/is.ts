export interface fun { (value: any): boolean }

export interface arr { (value: any): boolean }

export interface obj { (value: any): boolean }

export interface str { (value: any): boolean }

export interface num { (value: any): boolean }

export interface bool { (value: any): boolean }

export interface numeric { (value: any): boolean }
